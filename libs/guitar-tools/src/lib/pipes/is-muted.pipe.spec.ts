/* tslint:disable:no-unused-variable */

import { IsMutedPipe } from './is-muted.pipe';

describe('Pipe: IsMutede', () => {
  it('create an instance', () => {
    const pipe = new IsMutedPipe();
    expect(pipe).toBeTruthy();
  });
});
