/* tslint:disable:no-unused-variable */

import { FretDotPipe } from './fret-dot.pipe';

describe('Pipe: FretDote', () => {
  it('create an instance', () => {
    const pipe = new FretDotPipe();
    expect(pipe).toBeTruthy();
  });
});
