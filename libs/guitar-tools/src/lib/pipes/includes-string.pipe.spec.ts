/* tslint:disable:no-unused-variable */

import { IncludesStringPipe } from './includes-string.pipe';

describe('Pipe: IncludesStringe', () => {
  it('create an instance', () => {
    const pipe = new IncludesStringPipe();
    expect(pipe).toBeTruthy();
  });
});
