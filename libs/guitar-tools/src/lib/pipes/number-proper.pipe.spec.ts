/* tslint:disable:no-unused-variable */

import { NumberProperPipe } from './number-proper.pipe';

describe('Pipe: NumberPropere', () => {
  it('create an instance', () => {
    const pipe = new NumberProperPipe();
    expect(pipe).toBeTruthy();
  });
});
