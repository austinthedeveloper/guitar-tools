/* tslint:disable:no-unused-variable */

import { StringTypePipe } from './string-type.pipe';

describe('Pipe: StringTypee', () => {
  it('create an instance', () => {
    const pipe = new StringTypePipe();
    expect(pipe).toBeTruthy();
  });
});
