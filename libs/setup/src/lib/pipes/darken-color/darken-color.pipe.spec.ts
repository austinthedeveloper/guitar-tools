import { DarkenColorPipe } from './darken-color.pipe';

describe('DarkenColorPipe', () => {
  it('create an instance', () => {
    const pipe = new DarkenColorPipe();
    expect(pipe).toBeTruthy();
  });
});
