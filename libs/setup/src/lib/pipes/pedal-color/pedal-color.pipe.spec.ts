import { PedalColorPipe } from './pedal-color.pipe';

describe('PedalColorPipe', () => {
  it('create an instance', () => {
    const pipe = new PedalColorPipe();
    expect(pipe).toBeTruthy();
  });
});
