import { GetTextColorPipe } from './get-text-color.pipe';

describe('GetTextColorPipe', () => {
  it('create an instance', () => {
    const pipe = new GetTextColorPipe();
    expect(pipe).toBeTruthy();
  });
});
